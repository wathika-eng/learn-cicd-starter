name: ci

on:
  pull_request:
    branches: [main]

jobs:
  style:
    name: Format and Lint
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Run go fmt
        run: go fmt ./...

  tests:
    name: Tests
    runs-on: ubuntu-latest
    needs: style

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Run tests
        run: go test ./... -cover

      # - name: Force Failure
      #   run: (exit 1)
